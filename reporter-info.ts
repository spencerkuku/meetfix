// Toast shown when reporter-info (class/department + phone) is incomplete —
// the completeness check itself lives in repair-visibility's
// isReporterInfoComplete, shared with the backend. This message is
// frontend-only display text.
export const REPORTER_INFO_REQUIRED_MESSAGE = '請填寫班級/部門與聯絡電話';
